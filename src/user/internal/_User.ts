/** The class that is used to define a User */
class _User {
    /** The user's ID */
    public readonly id: string;

    /**
     * Constructor for _User class
     * @param id ID of User
     */
    constructor(id: string) {
        this.id = id;
    }
}

export default _User;